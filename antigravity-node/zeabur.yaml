name: antigravity-node4

build:
  type: dockerfile
  dockerfile: Dockerfile

deploy:
  port: 8080
  healthcheck:
    path: /health
    interval: 30
    timeout: 5

env:
  PORT: "8080"
  NODE_ENV: production
  # ANTIGRAVITY_ACCOUNTS: set via Zeabur dashboard

resources:
  cpu: 0.1
  memory: 512
